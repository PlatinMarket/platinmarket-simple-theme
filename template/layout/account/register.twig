{# Use generic blocks #}
{% use "/block/generic.twig" %}
{% use "/block/header.twig" %}
{% use "/block/footer.twig" %}

<!DOCTYPE HTML>
<html lang="en">
<head>
  <title>{{ block('title') }}</title>
  {{ block('meta') }}
  {{ block('style') }}
</head>
<body>
{{ block('header') }}

<div class="container">

  <div class="card mb-3">
    <div class="card-header bg-white pl-5 pr-5 pt-4 pb-4">
      <h3 class="m-0">
        Hesap Oluştur
      </h3>
      <p class="card-text text-muted">Yeni misin ? Aşağıdan hesap oluşturabilirsin. Zaten üye misin ? <a href="/account/login"><strong>Giriş Yap</strong></a></p>
    </div>
    <div class="pl-4 pr-4">
      <div class="row">
        <div class="col-6">
          <div class="p-4">
            <form method="post" autocomplete="off">
              <div class="row">
                <div class="form-group col-6 {% if error_fields %}has-danger{% endif %}">
                  <label class="form-control-label">Adınz</label>
                  <input type="text" value="{{ register.firstname }}" class="form-control form-control-danger" name="firstname" required />
                  <small class="form-control-feedback">hata</small>
                </div>
                <div class="form-group col-6">
                  <label class="form-control-label">Soyadınız</label>
                  <input type="text" value="{{ register.lastname }}" class="form-control" name="lastname" required />
                </div>
              </div>
              <div class="form-group">
                <label class="form-control-label">E-posta adresi</label>
                <input type="email" value="{{ register.email }}" class="form-control" name="email" />
              </div>
              <div class="form-group">
                <label class="form-control-label">Şifre</label>
                <input type="password" value="{{ register.password }}" class="form-control" name="password" required />
              </div>
              <div class="form-group">
                <label class="form-control-label">Şifre tekrar</label>
                <input type="password" value="{{ register.password }}" class="form-control" name="password" required />
              </div>
              <div class="form-group">
                <label class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input">
                  <span class="custom-control-indicator"></span>
                  <span class="custom-control-description">Önemli kampanyalardan e-posta ile haberdar olmak istiyorum</span>
                </label>
                <label class="custom-control custom-checkbox">
                  <input type="checkbox" class="custom-control-input">
                  <span class="custom-control-indicator"></span>
                  <span class="custom-control-description">Önemli kampanyalardan SMS ile haberdar olmak istiyorum</span>
                </label>
              </div>
              <button type="submit" class="btn btn-block btn-primary mb-3">Hesabımı Oluştur</button>
              <small class="form-text text-muted">
                Hesap oluşturarak <a href=""><b>üyelik sözleşmesini</b></a> okuduğunuzu ve kabul ettiğinizi onaylıyorsunuz.
              </small>
            </form>

            {% if error_fields %}
              <ul>
                {% for field in error_fields  %}
                  <li>{{ field.field }} {{ field.message }} </li>
                {% endfor %}
              </ul>

            {% endif %}
          </div>
        </div>
        <div class="col-6">
          <div class="p-4">
            <p class="text-muted">
              Aşağıda ki sosyal medya hesaplarınız üzerinden mağazamıza üye olabilir ve sonrasında yine aynı sosyal medya hesabıyla giriş yapabilirsiniz.
            </p>
            <hr/>

            <a href="" class="btn btn-block btn-info">Facebook ile bağlan</a>
            <a href="" class="btn btn-block btn-danger">Google ile bağlan</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>


{{ block('footer') }}
{{ block('script') }}

</body>
</html>
